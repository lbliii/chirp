# Font Configuration
# ==================
# Google Fonts for the site CSS and for social card (OG image) PNG rendering
# (Pillow loads the same downloaded TTFs).

fonts:
  display: "Inter:400,600,700"
