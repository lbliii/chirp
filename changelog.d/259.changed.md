Pelt now drains failed query exchanges through `ReadyForQuery` before raising, so pooled connections reliably roll back failed transactions before reuse. Its free-threading contract also has no-GIL contention and decode-overlap gates, live PostgreSQL pool/cache proof, and an auditable evidence map; current database docs identify the in-tree pure-Python driver instead of the retired asyncpg backend.
