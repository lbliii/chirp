Add an explicit experimental `WebMCPForm` projection that renders safely escaped declarative browser-tool attributes from frozen dataclass form contracts while preserving ordinary HTTP, htmx, validation, and CSRF behavior.
