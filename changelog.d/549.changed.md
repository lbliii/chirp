Htmx 4 preview requests now fail before send when a response contains removed `HX-Trigger-After-Swap` or `HX-Trigger-After-Settle` headers. Htmx 2 and generic wire behavior remains unchanged; preview applications migrate to rendered target data and per-target settle lifecycle events.
