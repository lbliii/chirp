**Pounce 0.7 operator guidance** — documented the `bengal-pounce>=0.7.0` production boundary, paired `chirp check` with `pounce check`, described Pounce config inspection commands, and clarified that `pounce.toml` is Pounce-native today while `app.run()` and `chirp run` use `AppConfig`.
