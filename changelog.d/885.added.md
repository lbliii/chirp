**Architecture gap diagnostics** — private inspection now projects existing `dead` / `orphan` / `unreachable_block` findings, severity overrides as `suppressed` (never clean), and unproven/unobserved dynamic or behavioral gaps without promoting those categories to ERROR.
