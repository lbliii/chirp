**Skill freeze-time manifest** — `use_skill` registers the skill as an app
domain; at `app.freeze()` an immutable `Manifest` is finalized with a
`content_digest` over tool schemas + template sources + public key.


