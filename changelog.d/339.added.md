Published the universal-operation design boundary for projecting one typed Milo command across Chirp HTTP, htmx, CLI, MCP, WebMCP, and MCP Apps without adding automatic exposure, a parallel renderer, or a generic JSON path.
