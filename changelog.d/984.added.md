**Env-var keystore + key-status** — `chirp.skill.EnvKeystore` resolves
provider keys from the environment by name (manifests declare names only).
The host-level `key-status` MCP tool reports presence without values and
runs a leak guard so secrets never enter Envelope/MCP responses.
Provisional submodule API.
