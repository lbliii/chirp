Fresh-environment install and import smoke covers every supported dependency
resolution profile (decision #908) via `scripts/install_smoke.py` and the
dedicated `install-smoke` GitHub Actions workflow. Failures name the profile ID
and resolution path; ordinary `uv sync --group dev` stays unchanged.
