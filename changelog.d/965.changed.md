**MCP 2026-07-28 stateless core** — `/mcp` no longer requires an
`initialize` / `notifications/initialized` handshake. Per-request
``params._meta`` carries protocol version, client identity, and
capabilities; optional ``server/discover`` advertises supported versions.
Legacy initialize remains an accept-and-noop for older clients.
