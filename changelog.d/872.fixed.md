Access-grant persistence is portable across SQLite and PostgreSQL: ``access_grants_ddl(dialect)`` emits dialect-correct primary-key SQL, and ``create_grant`` reads the inserted row via ``INSERT ... RETURNING`` instead of SQLite ``rowid`` / ``last_insert_rowid()``.
