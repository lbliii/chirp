**Skill Envelope return type** — `chirp.skill.Envelope` is a frozen signed
result envelope (Ed25519). Handlers may return it; `negotiate()` emits the
wire JSON, and `verify_envelope()` detects tampering. Provisional submodule
API (not re-exported from top-level `chirp`).
