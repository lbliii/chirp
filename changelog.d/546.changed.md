Request htmx metadata now normalizes htmx 4 `tag#id` targets and sources alongside htmx 2 headers, exposes validated `HX-Request-Type`, varies negotiated pages by request type, and extends `TestClient` with htmx 4 request metadata helpers.
