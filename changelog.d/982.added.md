**Skill hypermedia console** — `chirp.skill.mount_console` registers browse +
detail pages (`Page`/`Fragment`) over a `SkillRegistry`, showing manifest,
contract summary, and smoke `ReliabilityScore`. Live-log and keystore hooks
are left for sibling issues.
