The optional `benchmark` extra raises its FastHTML floor to
`python-fasthtml>=0.14.11`, so resolution no longer pulls the yanked
`apsw==3.53.3.0` transitive chain through `fastlite` / `apswutils`. Chirp
still does not declare APSW as a runtime dependency.
