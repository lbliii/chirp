`chirp new` now emits a deterministic, app-owned modular Kida scaffold with explicit component and pattern roots; installed optional packages no longer change default or shell output, while `--with-chirpui` remains the explicit compatibility choice.
