**App-owned semantic tokens and theme selection** — `chirp new` scaffolds layered `static/css` (`tokens`, `base`, `components`, `patterns`, `pages`), root `data-theme` light/dark/system with cookie-backed preference, and progressive `theme.js` enhancement without Alpine, inline scripts, or a CSS-in-Python API (#858).
