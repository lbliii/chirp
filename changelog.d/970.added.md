**Skill mount + Manifest** — `chirp.skill.Skill` / `use_skill(app, skill)`
mount `@skill.tool` handlers onto the app MCP registry with signed `Envelope`
results. `Manifest` serializes skill identity (name, semver, tools, public
key, provider-key names, content digest). Provisional submodule API.
