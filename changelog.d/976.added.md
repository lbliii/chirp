**Skill publish gate** — `chirp skill publish` (and `chirp.skill.publish.run_publish_gate`)
runs check + freeze + smoke and emits a pass/fail receipt; any failing stage
blocks publish. No registry upload.
