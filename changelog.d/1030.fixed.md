MCP tool calls validate required arguments, types, and array items against their advertised input schema before invoking handlers. Invalid inputs return JSON-RPC `-32602` with an actionable argument path instead of reaching domain code. Union annotations retain all supported alternatives, and postponed annotations are resolved when generating tool schemas so typed calls keep working.
