**MCP legacy `2024-11-05` deprecation offramp** — Handshake-era MCP clients are
bridged (not hard-errored) through ``2027-07-28``. Legacy requests emit
``DeprecationWarning``; ``initialize`` responses include a structured
``chirp/legacyOfframp`` note; ``app.check()`` surfaces INFO ``mcp_legacy`` when
tools are registered. SEP-2243 header enforcement stays gated to modern protocol
advertisements.
