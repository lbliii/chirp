Chirp DevTools, islands, safe-target processing, and View Transitions now consume both htmx 2 lifecycle events and htmx 4 fetch-era events. Request-context correlation prevents duplicate diagnostics and island effects when the `htmx-2-compat` extension emits both event names.
