Pelt pools now share a process-wide immutable type-catalog cache keyed by host/port/database. After the first checkout warms `pg_catalog` metadata, later checkouts hydrate connection-local codecs from the snapshot without re-querying; invalidation happens on explicit pool reset or when the last pool for that database closes.
