PostgreSQL server-side streams schedule parallel row decoding in ordered chunks to reduce per-row executor overhead on free-threaded Python. Socket access remains serial and connection-owned; row order and the configured batch memory bound are preserved.
