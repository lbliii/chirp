PostgreSQL TLS connections now explicitly negotiate as clients, allowing managed PostgreSQL services such as Railway to work with Pelt's default SSL mode and preserving hostname verification for `sslmode=verify-full`.
