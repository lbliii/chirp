**Skill packaging** — optional `chirp[skill]` extra pulls `cryptography` for
Ed25519 envelopes. `chirp.skill` remains a provisional submodule API (documented
in `docs/public-api.md`, not re-exported from top-level `chirp`); `import chirp`
and `import chirp.skill` stay free of cryptography until sign/verify/mount.
