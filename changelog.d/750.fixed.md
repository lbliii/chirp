**Quota-aware production worker auto-detect** — when ``AppConfig.workers=0``, Chirp resolves a concrete worker count from cgroup CPU quota/cpuset (and optional ``WEB_CONCURRENCY``) before launching Pounce, so container/Railway services no longer inherit the unrelated host CPU count. Explicit ``workers=N`` stays authoritative; startup prints a ``chirp workers:`` diagnostic.
