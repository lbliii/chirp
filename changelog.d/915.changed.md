CI capability lanes prove ``chirp[config]`` (``.env`` via ``AppConfig.from_env``)
and ``chirp[ai-bedrock]`` (credential-free Botocore SigV4 signing against a mock
transport) without promoting those extras into the default development profile.
``AppConfig.from_env()`` now loads ``.env`` from the process working directory
when ``python-dotenv`` is installed, matching its documented contract.
