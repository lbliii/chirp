Chirp no longer ambiently discovers chirp-ui templates or filters from package presence; apps activate chirp-ui only through explicit ``use_chirp_ui(app)`` (or an equivalent ``App.add_loader`` + filter registration), while ``chirp new`` remains package-presence-independent and the ``chirpui_runtime`` check flags stale implicit imports.
