`verify_and_upgrade` now accepts keyword-only `upgrade_algorithm: bool = False` and forwards it to `needs_rehash`, so scrypt→argon2 migration is an explicit opt-in (storm-safe by default) that matches the documented contract.
