**Orrery Railway dogfood** — `examples/standalone/orrery` mounts N wrapped
skills (`gaze` / `resolve` / `star`) via `mount_skills` + `mount_console`,
serves aggregated `/mcp` + discovery, streams live invocations on `/`, and
ships `Dockerfile` / `railway.toml` for a Railway deploy.
