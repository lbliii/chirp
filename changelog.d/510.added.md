Published the structured application-inspection design for one immutable result shared by Python consumers, terminal checks, versioned JSON, and contract diff tooling while preserving the existing CLI JSON contract.
