The exact htmx 4 preview now gives `EventStream` a native, connection-fixed SSE dialect: rendered fragments use unnamed HTML and validated `<hx-partial>` targets, named `SSEEvent` values remain DOM events, `sse_scope()` emits `hx-sse:connect`, and tests and DevTools expose the selected wire contract. Htmx 2 and generic SSE behavior remains available during migration.
