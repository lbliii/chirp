**MCP docs for 2026-07-28 Streamable HTTP** — The Tools & MCP guide and
public-API audit note now describe the stateless `/mcp` transport, per-request
``params._meta``, and SEP-2243 routing headers (``MCP-Protocol-Version``,
``Mcp-Method``, ``Mcp-Name``) with ``HeaderMismatch`` (``-32020``) semantics.
