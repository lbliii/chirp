Skill console reliability badges use a middle-dot separator (``5/7 · fail``)
so the pass count is not read as a failure count, and the Orrery dogfood
example records per-skill smoke slices instead of one host-wide report.
