Redis-backed sessions now preserve passkey ceremony challenges and session timeout stamps instead of dropping every ``__``-prefixed key, and cookie sessions remain the first-class low-infrastructure path for passkeys without a production warning that pushes Redis.
