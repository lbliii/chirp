Specialized CI capability lanes set ``CHIRP_CAPABILITY_LANE`` so unexpected
skips or missing required selectors fail the job with diagnostics naming the
missing dependency or service, while ordinary local and default-unit runs keep
soft optional skips.
