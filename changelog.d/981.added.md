**Skill registry + discovery** — `chirp.skill.SkillRegistry` / `mount_skills(app, skills)`
store skills by name, mount each via `use_skill` onto one aggregated `/mcp`, and
expose a discovery route (default `/skills`) listing manifests. Provisional
submodule API.
