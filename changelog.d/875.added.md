`AppConfig.display` forwards an optional Pounce `DisplayConfig` through `app.run()`, `chirp run`, and the dev/production launchers as `ServerConfig.display`, so apps can supply typed startup identity without using slotted-App ambient hooks.
