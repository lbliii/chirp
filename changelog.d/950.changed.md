**Suspense + Pelt checkout isolation** — Independent Suspense defers that
acquire from the Pelt pool hold distinct connections under concurrent drain;
pool exhaustion queues on `acquire()` (bounded wait) rather than sharing a
borrow. Documented with `pool_size` sizing guidance for Suspense pages.
