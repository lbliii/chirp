**Live invocation log** — `chirp.tools.tool_event_stream` /
`mount_invocation_log` bridge `ToolEventBus` into an `EventStream` of live-log
fragments (default `/invocations/live`). `mount_skills` wires the route by
default; pass `invocation_log_path=None` to disable. No historical persistence.
