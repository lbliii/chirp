Added stable compiled-transition IDs and public-safe descriptions to debug return traces, DevTools exports, and testing coverage helpers so normal, boosted, targeted, mutation, OOB, Suspense, and SSE paths can share runtime evidence.
