# Search

A GET-driven book search example with htmx. It shows how to keep the URL as the
source of truth while still providing live search, filtering, sorting, and
fragment updates.

## Run

```bash
cd examples/search && python app.py
```

## Test

```bash
pytest examples/search/
```
