# Accessibility

A feedback-form example focused on accessible server-rendered patterns:
landmarks, labels, descriptions, validation messaging, live regions, and clear
keyboard-focus behavior.

## Run

```bash
cd examples/accessibility && python app.py
```

## Test

```bash
pytest examples/accessibility/
```
