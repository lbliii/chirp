# Tools

An MCP tools example that exposes Python functions to both browser users and AI
clients. It also streams a live activity feed so tool calls show up in the UI
in real time.

## Run

```bash
PYTHONPATH=src python examples/standalone/tools/app.py
```

## Test

```bash
pytest examples/standalone/tools/
```
