# Signup

A registration flow that demonstrates Chirp's form validation helpers,
`ValidationError`, CSRF protection, and session-backed success flow. It is a
focused reference for server-rendered forms with field-level errors.

## Run

```bash
PYTHONPATH=src python examples/standalone/signup/app.py
```

## Test

```bash
pytest examples/standalone/signup/
```
