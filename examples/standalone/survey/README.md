# Survey

A forms showcase covering multiple input types in one app: text, number,
checkboxes, radios, selects, and textareas. It demonstrates built-in validators
plus multi-value field handling with checkbox lists.

## Run

```bash
PYTHONPATH=src python examples/standalone/survey/app.py
```

## Test

```bash
pytest examples/standalone/survey/
```
