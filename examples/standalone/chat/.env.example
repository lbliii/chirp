# Session secret — CHANGE THIS in production
SESSION_SECRET_KEY=change-me-in-production

# Optional — only if you load config with AppConfig.from_env():
# CHIRP_LOG_FORMAT=json   # structured logs in containers / log aggregators
