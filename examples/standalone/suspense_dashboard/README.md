# Suspense Dashboard

Demonstrates Chirp's `Suspense` return type: the page shell renders instantly with skeleton placeholders, then slow data sources fill in as OOB swaps.

## Run

```bash
python app.py
```

## Test

```bash
pytest test_app.py
```
