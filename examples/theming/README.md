# Theming

A dark/light mode example using only platform primitives: CSS custom
properties, `prefers-color-scheme`, a `data-theme` override, and a tiny
anti-FOUC script. It is a good reference for framework-free app theming.

## Run

```bash
cd examples/theming && python app.py
```

## Test

```bash
pytest examples/theming/
```
